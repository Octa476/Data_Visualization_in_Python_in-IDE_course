# TO DO: create your own bar chart

# These prints are used by the checker and you need to specify the axis of the plot "x" and the "hue".
# When the checker runs close the plots that appear and then the script will verify if the prints are the
# same with the reference solution's prints(this information fully determine if the two plots are the same) 
print(df)
print(df['genre'].cat.categories)
print(df['platform'].cat.categories)
print(x)
print(hue)
